<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newsapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Science" android:label="Science"></activity>
        <activity android:name=".Sports" android:label="Sports"/>
        <activity android:name=".Entertainment" android:label="Entertainment" />
        <activity android:name=".Bussinus" android:label="Business"/>
        <activity android:name=".Technology" android:label="Technology"/>
        <activity android:name=".Health" android:label="Health" />
        <activity android:name=".MainActivity" android:label="News">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>